name = "android"
markers = ["settings.gradle", "settings.gradle.kts", "build.gradle", "build.gradle.kts"]
fallback = "generic"
tool_markers = ["gradlew"]

[targets]
unit = "test"
full = "validate"

[commands]
lint = ["./gradlew", "lint"]
test = ["./gradlew", "test"]
build = ["./gradlew", "assemble"]
