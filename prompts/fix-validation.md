Read AGENTS.md, {spec_path}, {plan_path}, and {tasks_path}.

Repair only this task without discarding the existing diff:
{task_id}: {task_title}

Related requirements: {requirements}
Named validations: {validations}

Validation or execution failure:
{failure}

Do not change the specification, weaken tests, read secret files, push, merge,
or use destructive Git commands. Do not update the task checkbox or validation
log. Keep all changes inside the specification's Allowed Changes scope.
