Read AGENTS.md, {spec_path}, {plan_path}, and {tasks_path}.

Implement only this task:
{task_id}: {task_title}

Related requirements: {requirements}
Named validations: {validations}

Do not implement another task, change the specification, weaken tests, read
secret files, push, merge, or use destructive Git commands. Do not update the
task checkbox or validation log; the orchestrator owns task state and evidence.
Keep all changes inside the specification's Allowed Changes scope.
