version = 1
default_branch = "main"
allowed_make_targets = ["setup", "format-check", "lint", "typecheck", "test", "build", "integration-test", "validate"]
max_tasks = 50
max_attempts_per_task = 5
max_review_attempts = 3
max_ci_attempts = 3
max_elapsed_minutes = 120
auto_merge_low_risk = false
allow_legacy_contracts = false
queue_concurrency = 1
max_codex_calls = 20
max_review_calls = 8
max_ci_repairs = 3

[quality.lint]
enabled = true

[quality.typecheck]
enabled = true

[quality.test]
enabled = true

[quality.build]
enabled = true

[notifications]
sinks = ["stdout", "outbox"]

[risk_paths]
high = ["auth/**", "security/**", "migrations/**", "deploy/**", "**/production.*"]
medium = [".github/**", "Dockerfile", "Makefile", "**/Dockerfile"]
